"""LangGraph-based analysis orchestration for OASIS.

Import from submodules explicitly, e.g. ``from oasis.agent.invoke import invoke_oasis_langgraph``,
so that importing ``oasis.agent`` alone does not load LangGraph at interpreter startup.
"""
